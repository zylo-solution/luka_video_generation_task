# Example environment configuration for the AI video generator.
#
# Copy this file to `.env` and replace the placeholder values with your
# personal API keys. Only keys relevant to your chosen providers need
# to be populated. If a key is missing, the corresponding feature will
# gracefully fall back to deterministic defaults.

GEMINI_API_KEY=your-gemini-api-key-here
HEYGEN_API_KEY=your-heygen-api-key-here
SUBMAGIC_API_KEY=your-submagic-api-key-here

# Optional: specify a voice key for ElevenLabs if you plan to use an
# external TTS provider. Currently unused in this implementation.
ELEVENLABS_API_KEY=your-elevenlabs-api-key-here

# Replicate, Luma and other API tokens are not used by this project but
# can be included for future extensions.
REPLICATE_API_TOKEN=
LUMA_API_KEY=